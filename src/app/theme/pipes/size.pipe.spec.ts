/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SizePipe } from './size.pipe';

describe('Pipe: Sizee', () => {
  it('create an instance', () => {
    let pipe = new SizePipe();
    expect(pipe).toBeTruthy();
  });
});
